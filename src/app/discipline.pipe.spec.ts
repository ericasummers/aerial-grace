import { DisciplinePipe } from './discipline.pipe';

describe('DisciplinePipe', () => {
  it('create an instance', () => {
    const pipe = new DisciplinePipe();
    expect(pipe).toBeTruthy();
  });
});
